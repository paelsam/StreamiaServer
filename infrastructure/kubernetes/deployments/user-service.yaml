apiVersion: apps/v1
kind: Deployment
metadata:
  name: user-service
  namespace: streamia
  labels:
    app: streamia
    service: user-service
spec:
  replicas: 2
  selector:
    matchLabels:
      app: streamia
      service: user-service
  template:
    metadata:
      labels:
        app: streamia
        service: user-service
    spec:
      containers:
        - name: user-service
          image: andresmg42/streamia-user-service:latest
          ports:
            - containerPort: 3001
          # Carga todas las variables del ConfigMap
          envFrom:
            - configMapRef:
                name: streamia-config
            # También carga todos los secrets
            - secretRef:
                name: streamia-secrets
          env:
            # Variables específicas de este servicio
            - name: PORT
              value: "3001"
            # Mapea MONGODB_URI desde el secret específico
            - name: MONGODB_URI_USERS
              valueFrom:
                configMapKeyRef:
                  name: streamia-config
                  key: MONGODB_URI_USERS
          resources:
            requests:
              memory: "128Mi"
              cpu: "100m"
            limits:
              memory: "512Mi"
              cpu: "500m"
          livenessProbe:
            httpGet:
              path: /health/live
              port: 3001
            initialDelaySeconds: 15
            periodSeconds: 20
            timeoutSeconds: 5
            failureThreshold: 3
          readinessProbe:
            httpGet:
              path: /health/ready
              port: 3001
            initialDelaySeconds: 5
            periodSeconds: 10
            timeoutSeconds: 3
            failureThreshold: 3
---
apiVersion: v1
kind: Service
metadata:
  name: user-service
  namespace: streamia
  labels:
    app: streamia
    service: user-service
spec:
  selector:
    app: streamia
    service: user-service
  ports:
    - port: 3001
      targetPort: 3001
      protocol: TCP
  type: ClusterIP
---
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: user-service-hpa
  namespace: streamia
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: user-service
  minReplicas: 2
  maxReplicas: 10
  metrics:
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: 70
    - type: Resource
      resource:
        name: memory
        target:
          type: Utilization
          averageUtilization: 80
